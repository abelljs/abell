{
  "name": "abell-renderer",
  "version": "0.0.0",
  "description": "",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node ./src/example.ts",
    "dev": "yarn dev:with-esbuild",
    "dev:with-esbuild": "nodemon -e ts -w ./src -x yarn esbuild",
    "dev:with-tsnode": "nodemon -e ts -w ./src -x yarn start",
    "ts-node": "ts-node",
    "esbuild": "esbuild src/example.ts --bundle --outfile=dev/example.js --platform=node --target=node14 && node dev/example.js",
    "build": "tsc",
    "serve": "yarn build && node dist/example.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "acorn": "^8.3.0",
    "htmlparser2": "^6.1.0"
  }
}
